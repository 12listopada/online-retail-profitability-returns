## Power BI Report

Power BI was used for data modeling, DAX calculations and dashboard creation.

The final report is exported to PDF and available in the `docs/` folder.

The PBIX file is not included to keep the repository lightweight.
It can be shared upon request.
